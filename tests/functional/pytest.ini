[pytest]
markers =
    functional: live-environment S-FUNC suite (ADR-0045); needs deployed env + credentials
testpaths = .
python_files = test_*.py
addopts = -ra
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
